import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uao.parcial2.misiones',
  appName: 'Misiones Parcial 2',
  webDir: 'dist'
};

export default config;
